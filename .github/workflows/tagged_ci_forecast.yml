name: Tagged CI - Forecast Inference
run-name: 'Tagged CI (Forecast) for ${{ github.ref_name }} by ${{ github.actor }}'
on:
  push:
    tags: ["v*.*.*"]
permissions:
  packages: write
  contents: read
  id-token: write
jobs:
  tagged-ci-forecast-inference:
    uses: openclimatefix/.github/.github/workflows/tagged_ci.yml@main
    secrets: inherit
    with:
      containerfile: "infrastructure/Dockerfile.forecast-inference"
